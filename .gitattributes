# Normalize text files in the repository, but pin the line endings that
# actually matter on disk. cmd.exe misparses a .bat file that uses LF.
* text=auto

*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

*.py  text eol=lf
*.md  text eol=lf

*.png binary
